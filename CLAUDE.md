# personal-ai-employee Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-01-26

## Active Technologies
- Python 3.13+ (mandated by constitution and feature requirements) + watchdog (filesystem monitoring), pathlib (path operations), python-dotenv (environment config), logging (monitoring) (main)
- Local Markdown files in Obsidian vault structure (Inbox, Needs_Action, Done directories) (main)
- Python 3.13+ (as mandated by constitution v3.0.0) + watchdog (filesystem monitoring), pathlib (path operations), python-dotenv (environment config), logging (monitoring) (main)
- Python 3.13+ (as mandated by constitution; 3.14 available on this machine) + watchdog (filesystem), python-dotenv (config), requests (HTTP), playwright (WhatsApp), xmlrpc.client (Odoo, stdlib), tweepy (Twitter API v2) (004-gold-tier)
- Local Obsidian vault (Markdown files + JSON) — no database (004-gold-tier)

- Python 3.13+ + pathlib, watchdog (003-bronze-ai-employee)

## Project Structure

```text
src/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

Python 3.13+: Follow standard conventions

## Recent Changes
- 004-gold-tier: Added Python 3.13+ (as mandated by constitution; 3.14 available on this machine) + watchdog (filesystem), python-dotenv (config), requests (HTTP), playwright (WhatsApp), xmlrpc.client (Odoo, stdlib), tweepy (Twitter API v2)
- main: Added Python 3.13+ (as mandated by constitution v3.0.0) + watchdog (filesystem monitoring), pathlib (path operations), python-dotenv (environment config), logging (monitoring)
- main: Added Python 3.13+ (mandated by constitution and feature requirements) + watchdog (filesystem monitoring), pathlib (path operations), python-dotenv (environment config), logging (monitoring)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
